<%= form_for :comment, url:comments_path do |f| %>
    <%= f.text_field :text, class:"inputt"%>
    <%= f.hidden_field :work_id, value: @work.id%>
    <%= f.submit "Comment", class:"btn btn-dark"%>
    <%end%>